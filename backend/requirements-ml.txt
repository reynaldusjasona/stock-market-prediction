# Extra ML dependencies for sentiment scoring (GitHub Actions only).
# These are NOT installed on Render — FinBERT + PyTorch exceed the
# free-tier 512 MB RAM limit.  The sentiment cron workflow installs
# requirements.txt (base) first, then this file on top.

transformers>=4.40.0
torch>=2.5.0
